#include <iostream>
using namespace std;   // This will include the standard library to all the builtin function.

int main()
{
    int x, y;
    cout << "Enter the value of x: ";
    cin >> x;
    cout << "Enter the value of y: ";
    cin >> y;
    cout << "The value of x is: " << x << " & The value of y is: " << y << endl;
    cout << "Their sum is: " << (x+y);

    return 0;
}